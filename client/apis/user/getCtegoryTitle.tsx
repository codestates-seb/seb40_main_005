/**
 * @author yeowool
 * @description login token을 인증하여 get으로 사용자의 전체 카테고리 목록을 return
 **/

const getCtegoryTitle = () => {
  // 요청메소드 + 요청정보
  // return client.get("/category").then(response => {
  //   return response.data;
  // });
};

export default getCtegoryTitle;
